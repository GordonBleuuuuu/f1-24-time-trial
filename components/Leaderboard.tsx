import { DriverLap, formatDelta, formatMs } from "@/lib/telemetry";
type Props = { laps: DriverLap[]; identity?: { name: string; gamertag: string; input: string } };
const sectorClass = (value: number, values: number[], personalBest: number) => !value ? "text-zinc-500" : value === Math.min(...values.filter(Boolean)) ? "text-fuchsia-400" : personalBest && value <= personalBest ? "text-emerald-400" : "text-yellow-300";
export function Leaderboard({ laps, identity }: Props) {
  const ordered = [...laps].sort((a, b) => a.position - b.position || a.currentLapTime - b.currentLapTime), sector1 = ordered.map((lap) => lap.sector1), sector2 = ordered.map((lap) => lap.sector2), sector3 = ordered.map((lap) => lap.sector3), best = ordered.reduce((acc, lap) => Math.min(acc, lap.lastLapTime || Infinity), Infinity);
  return <section className="overflow-hidden border border-white/10 bg-panel/95"><div className="flex items-center justify-between border-b border-white/10 px-4 py-3"><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-f1">Live classification</p><h2 className="text-xl font-bold uppercase">Leaderboard</h2></div><span className="flex items-center gap-2 text-xs text-zinc-300"><i className="h-2 w-2 animate-pulse rounded-full bg-f1" /> LIVE</span></div><div className="overflow-x-auto"><table className="timing-grid w-full min-w-[670px] text-left font-timing text-sm"><thead className="bg-white/5 text-[10px] uppercase tracking-wider text-zinc-400"><tr><th className="px-4 py-3">Pos</th><th>Driver</th><th>Car</th><th>Lap</th><th>Current</th><th>S1</th><th>S2</th><th>S3</th><th>Delta</th></tr></thead><tbody>{ordered.length ? ordered.map((lap, index) => <tr key={lap.carIndex} className="border-t border-white/[.06] hover:bg-white/[.035]"><td className="border-l-4 border-f1 px-4 py-3 font-bold">{lap.position || index + 1}</td><td><b>{identity?.name || lap.driverName || "DRIVER"}</b><span className="ml-2 text-xs text-zinc-500">{identity?.gamertag}</span></td><td className="text-xs text-zinc-300">{lap.car}</td><td>{lap.currentLap}</td><td className="font-bold text-white">{formatMs(lap.currentLapTime)}</td><td className={sectorClass(lap.sector1, sector1, lap.personalBestSector1)}>{formatMs(lap.sector1, false)}</td><td className={sectorClass(lap.sector2, sector2, lap.personalBestSector2)}>{formatMs(lap.sector2, false)}</td><td className={sectorClass(lap.sector3, sector3, lap.personalBestSector3)}>{formatMs(lap.sector3, false)}</td><td className={lap.deltaToLeader <= 0 || lap.lastLapTime === best ? "text-fuchsia-400" : "text-zinc-300"}>{formatDelta(lap.deltaToLeader)}</td></tr>) : <tr><td colSpan={9} className="px-4 py-10 text-center text-zinc-500">Waiting for F1 24 UDP telemetry on port 20777…</td></tr>}</tbody></table></div></section>;
}
