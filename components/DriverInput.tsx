"use client";
import { FormEvent, useState } from "react";
import type { DriverDetails } from "@/lib/telemetry";
export function DriverInput({ onSave }: { onSave: (details: DriverDetails) => void }) {
  const [name, setName] = useState(""), [gamertag, setGamertag] = useState(""), [input, setInput] = useState("Wheel / Direct Drive");
  function submit(event: FormEvent) { event.preventDefault(); if (name.trim() && gamertag.trim()) onSave({ name: name.trim(), gamertag: gamertag.trim(), input, assistPreset: "Unspecified", platform: "PC" }); }
  return <div className="fixed inset-0 z-50 grid place-items-center bg-black/80 p-5 backdrop-blur-sm"><form onSubmit={submit} className="w-full max-w-md border border-white/15 bg-panel p-6 shadow-2xl"><div className="mb-6 border-l-4 border-f1 pl-3"><p className="text-xs uppercase tracking-[.22em] text-zinc-400">F1 24 · Time Trial</p><h1 className="text-3xl font-bold uppercase">Driver registration</h1></div><label className="mb-4 block text-xs font-bold uppercase tracking-wider text-zinc-300">Driver name<input required value={name} onChange={(e) => setName(e.target.value)} placeholder="Alex Morgan" className="mt-1 w-full border border-white/15 bg-black/30 px-3 py-3 text-base outline-none focus:border-f1" /></label><label className="mb-4 block text-xs font-bold uppercase tracking-wider text-zinc-300">Gamertag<input required value={gamertag} onChange={(e) => setGamertag(e.target.value)} placeholder="AMRacer" className="mt-1 w-full border border-white/15 bg-black/30 px-3 py-3 text-base outline-none focus:border-f1" /></label><label className="mb-6 block text-xs font-bold uppercase tracking-wider text-zinc-300">Input setup<select value={input} onChange={(e) => setInput(e.target.value)} className="mt-1 w-full border border-white/15 bg-black/30 px-3 py-3 text-base outline-none focus:border-f1"><option>Wheel / Direct Drive</option><option>Wheel / Belt Drive</option><option>Controller</option><option>Keyboard</option></select></label><button className="w-full bg-f1 py-3 text-sm font-bold uppercase tracking-[.15em] transition hover:bg-red-500">Start live session</button></form></div>;
}
